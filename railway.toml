[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["Chorus/orchestrator/**", "Chorus/pyproject.toml", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
